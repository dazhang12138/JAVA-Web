'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

/**
 * Created by Dio on 2016/7/8.
 * 固定侧边栏
 */
var React = require('react');
require('./style/index.less');
var YYClass = require('../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;
//定义META
var uiMeta = {
    name: 'fixedside',
    fields: [
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('position', 'left', PropTypes.oneOf(['left', 'right', 'top', 'bottom']), false, '固定方位')]
};
var YYFixedSide = YYClass.create({
    uiMeta: uiMeta,
    getInitialState: function getInitialState() {
        return {};
    },
    render: function render() {
        var cls = this.className() + ' ' + this.props.position;
        return React.createElement(
            'div',
            _extends({}, this.props, { className: cls }),
            this.props.children
        );
    }
});
module.exports = YYFixedSide;
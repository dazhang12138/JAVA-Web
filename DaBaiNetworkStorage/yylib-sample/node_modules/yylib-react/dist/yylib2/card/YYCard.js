'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

/**
 * 卡片组件
 */
var React = require('react');
var YYClass = require('../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

var _require = require('antd'),
    Card = _require.Card;
//定义META


var uiMeta = {
    name: 'card',
    fields: [
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('title', '', PropTypes.element, false, '卡片标题'), field('extra', '', PropTypes.element, false, '卡片右上角的操作区域'), field('bordered', true, PropTypes.bool, false, '是否有边框'), field('bodyStyle', {}, PropTypes.element, false, '内容区域自定义样式')]
};
var YYCard = YYClass.create({
    uiMeta: uiMeta,
    render: function render() {
        var cls = this.className();
        return React.createElement(
            Card,
            _extends({}, this.props, { className: cls }),
            this.props.children
        );
    }
});

module.exports = YYCard;
'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

/**
 * 定义栅格列
 */
var React = require('react');

var _require = require('antd'),
    Col = _require.Col;

var YYClass = require('../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;
//定义META
var uiMeta = {
    name: 'col',
    fields: [
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('span', undefined, PropTypes.number, false, '宽度比例，取1~24，确保一行Row总值为不超过24'), field('order', 0, PropTypes.number, false, '栅格顺序，flex 布局模式下有效'), field('offset', 0, PropTypes.number, false, '栅格左侧的间隔格数，间隔内不可以有栅格'), field('push', 0, PropTypes.number, false, '栅格向右移动格数'), field('pull', 0, PropTypes.number, false, '栅格向左移动格数')]
};
var YYCol = YYClass.create({
    uiMeta: uiMeta,
    render: function render() {
        return React.createElement(
            Col,
            _extends({}, this.props, { className: this.className() }),
            this.props.children
        );
    }
});
module.exports = YYCol;
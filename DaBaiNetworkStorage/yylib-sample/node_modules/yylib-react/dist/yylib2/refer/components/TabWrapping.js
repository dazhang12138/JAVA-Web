'use strict';

var React = require('react');
var YYTab = require('../../tabs/YYTab');
var YYClass = require('../../base/YYClass');

var TabWrappingComponent = function TabWrappingComponent(WrappedComponent, wrappedProps) {
    var WrappingComponent = YYClass.create({
        render: function render() {
            return React.createElement(
                YYTab,
                this.props,
                React.createElement(WrappedComponent, wrappedProps)
            );
        }
    });

    return WrappingComponent;
};

module.exports = TabWrappingComponent;
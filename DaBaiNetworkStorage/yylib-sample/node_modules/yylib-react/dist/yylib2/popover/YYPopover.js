'use strict';

/**
 * 气泡卡片组件
 */
var React = require('react');
var YYClass = require('../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

var _require = require('antd'),
    Popover = _require.Popover;

require('./style/index.less');
var uiMeta = {
    name: 'popover',
    fields: [
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('trigger', 'hover', PropTypes.oneOf(['hover', 'click', 'focus']), false, '触发行为'), field('placement', 'top', PropTypes.oneOf(['top', 'left', 'right', 'bottom', 'topLeft', 'topRight', 'bottomLeft', 'bottomRight', 'leftTop', 'leftBottom', 'rightTop', 'rightBottom']), false, '气泡框位置'), field('title', null, PropTypes.element, false, '卡片标题'), field('content', null, PropTypes.element, false, '卡片内容'), field('overlayClassName', null, PropTypes.string, false, '卡片类名'), field('overlayStyle', null, PropTypes.object, false, '卡片样式'), field('visible', undefined, PropTypes.bool, false, '用于手动控制浮层显隐'), field('onVisibleChange', null, PropTypes.func, false, '显示隐藏改变的回调'), field('getTooltipContainer', function () {
        return document.body;
    }, PropTypes.func, false, '菜单渲染父节点'), field('openClassName', '', PropTypes.string, false, '气泡框展现时触发器添加的类名,可用于打开浮层时高亮触发器')]
};
var YYPopover = YYClass.create({
    uiMeta: uiMeta,
    render: function render() {
        var cls = this.className();
        return React.createElement(
            'div',
            { className: cls },
            React.createElement(
                Popover,
                this.props,
                this.props.children
            )
        );
    }
});
module.exports = YYPopover;
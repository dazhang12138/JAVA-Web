'use strict';

/**
 * Created by Dio on 2016/5/11.
 */
var React = require('react');
var YYClass = require('../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

var _require = require('antd'),
    Tag = _require.Tag;

require('./style/index.less');
//定义META
var uiMeta = {
    name: 'tag',
    fields: [field('closable', false, PropTypes.bool, false, '标签是否可以关闭'), field('onClose', null, PropTypes.func, false, '关闭时的回调'), field('afterClose', null, PropTypes.func, false, '关闭动画完成后的回调'), field('color', '', PropTypes.oneOf(['blue', 'green', 'yellow', 'red']), false, '标签的色彩')]
};
var YYTag = YYClass.create({
    uiMeta: uiMeta,
    render: function render() {
        var cls = this.className();
        return React.createElement(
            'div',
            { className: cls },
            React.createElement(
                Tag,
                this.props,
                this.props.children
            )
        );
    }
});
module.exports = YYTag;
'use strict';

/**
 * Created by Dio on 2016/9/2.
 */
var React = require('react');
var YYClass = require('../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;
var YYPage = require('../container/YYPage');
var YYToolbar = require('../toolbar/YYToolbar');
var YYScrollbar = require('../scrollbar/YYScrollbar');
var YYRow = require('../layout/YYRow');
var YYCol = require('../layout/YYCol');
//左右布局的模板页面
var DoublePage = YYClass.create({
    getInitialState: function getInitialState() {
        return {};
    },
    render: function render() {
        return React.createElement(
            YYRow,
            null,
            this.props.toolbarPos == 'line' ? React.createElement(
                YYCol,
                { span: 24 },
                this.props.toolbar
            ) : null,
            React.createElement(
                YYCol,
                { span: 6, style: { paddingRight: 10, marginTop: this.props.toolbarPos != 'line' && !this.props.treeTop ? 0 : 0 } },
                this.props.treeTop ? React.createElement(
                    'div',
                    { style: { paddingTop: 3 } },
                    this.props.treeTop
                ) : null,
                this.props.treeTitle ? React.createElement(
                    'div',
                    { style: { background: '#f7f7f7', height: 40, lineHeight: '24px', padding: '8px 10px', fontSize: 14 } },
                    this.props.treeTitle
                ) : null,
                this.props.tree ? React.createElement(
                    YYScrollbar,
                    { style: { height: 650, background: '#fff' } },
                    this.props.tree
                ) : null
            ),
            React.createElement(
                YYCol,
                { span: 18 },
                this.props.toolbarPos != 'line' ? this.props.toolbar : null,
                this.props.table,
                this.props.cards
            )
        );
    }
});
module.exports = DoublePage;
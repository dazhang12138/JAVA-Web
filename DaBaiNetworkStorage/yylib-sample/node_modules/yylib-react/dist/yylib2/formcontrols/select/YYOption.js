'use strict';

/**
 * Created by Dio on 2016/5/11.
 */
var React = require('react');
var YYClass = require('../../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

var _require = require('antd'),
    Select = _require.Select;

var Option = Select.Option;
var uiMeta = {
    name: 'option',
    fields: [
    // field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('disabled', false, PropTypes.bool, false, '是否禁用'), field('key', undefined, PropTypes.string, false, '如果 react 需要你设置此项，此项值与 value 的值相同，然后可以省略 value 设置'), field('value', undefined, PropTypes.string, false, '默认根据此属性值进行筛选')]
};

var YYOption = YYClass.create({
    uiMeta: uiMeta
});

module.exports = YYOption;
'use strict';

/**
 * Created by Dio on 2016/5/11.
 */
var React = require('react');
var YYClass = require('../../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

var _require = require('antd'),
    Select = _require.Select;

var OptGroup = Select.OptGroup;
var uiMeta = {
    name: 'optgroup',
    fields: [
    // field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('label', undefined, PropTypes.oneOfType([PropTypes.element, PropTypes.string]), false, '组名'), field('key', undefined, PropTypes.string, false, '')]
};

var YYOptGroup = YYClass.create({
    uiMeta: uiMeta
    //,render: function () {
    //    return (
    //        <OptGroup {...this.props} className={this.className()}>
    //            {this.props.children}
    //        </OptGroup>
    //    );
    //}
});

module.exports = YYOptGroup;
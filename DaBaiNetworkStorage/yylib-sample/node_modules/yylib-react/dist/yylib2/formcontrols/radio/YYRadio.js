'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var React = require('react');
var YYClass = require('../../base/YYClass');
var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

var _require = require('antd'),
    Radio = _require.Radio;

require('./style/index.less');

var uiMeta = {
    name: 'radio',
    fields: [
    // field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('checked', undefined, PropTypes.bool, false, '指定当前是否选中'), field('defaultChecked', false, PropTypes.bool, false, '初始是否选中'), field('value', undefined, PropTypes.string, false, '根据 value 进行比较，判断是否选中')]
};

var YYRadio = YYClass.create({
    uiMeta: uiMeta
    //,statics:{
    //    Button:Radio.Button,
    //    Group:Radio.Group
    //}
    , getInitialState: function getInitialState() {
        return {};
    },
    render: function render() {
        return React.createElement(
            Radio,
            _extends({}, this.props, { prefixCls: this.className() + ' ant-radio' }),
            this.props.children
        );
    }
});
module.exports = YYRadio;
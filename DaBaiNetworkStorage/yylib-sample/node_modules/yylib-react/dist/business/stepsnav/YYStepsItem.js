'use strict';

var React = require('react');

var _require = require('yylib-ui'),
    YYClass = _require.YYClass;

var PropTypes = YYClass.PropTypes;
var field = YYClass.field;
//定义META
var uiMeta = {
    name: 'stepsnavitem',
    fields: [,
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('linkTarget', '', PropTypes.string, true, '依据此属性找到对应DOM,页面唯一'), field('title', [], PropTypes.string, true, 'step标题'), field('description', '', PropTypes.string, true, 'step详情描述'), field('icon', undefined, PropTypes.string, true, '显示的图标')]
};

var YYStepsNavItem = YYClass.create({
    uiMeta: uiMeta
});

module.exports = YYStepsNavItem;
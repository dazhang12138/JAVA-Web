'use strict';

var React = require('react');

var _require = require('yylib-ui'),
    YYIcon = _require.YYIcon;

require('./iconbutton.less');
var _ = require('lodash');

var CPIconButton = React.createClass({
    displayName: 'CPIconButton',

    getDefaultProps: function getDefaultProps() {
        return {
            icon: null,
            text: '操作',
            color: '',
            visible: true
        };
    },
    render: function render() {
        var cls = 'cp2-icontitle-button ' + this.props.color + (this.props.disabled == true ? ' disabled' : '');
        var style = this.props.style;

        return this.props.visible == false ? null : React.createElement(
            'div',
            { className: cls, style: style, onClick: this.props.disabled == true ? null : this.props.onClick },
            this.props.icon ? React.createElement(YYIcon, { type: this.props.icon }) : null,
            React.createElement(
                'span',
                null,
                this.props.text
            )
        );
    }
});

module.exports = CPIconButton;
'use strict';

var _require = require('./api.all'),
    FormApi = _require.FormApi,
    EditTableApi = _require.EditTableApi;

var _ = require('lodash');
var ESHandler = {
    "es.saveForm": {
        "onClick": function onClick() {
            FormApi.saveData(this);
        }
    },
    "es.addRow": {
        "onClick": function onClick(options) {
            EditTableApi.addRow(this);
        }
    },
    "es.delRow": {
        "onClick": function onClick() {
            EditTableApi.delRow(this);
        }
    }
};

module.exports = ESHandler;
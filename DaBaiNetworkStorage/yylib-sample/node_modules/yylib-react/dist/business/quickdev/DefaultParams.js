'use strict';

/**
 * Created by Dio on 2016/9/3.
 */
var _require$default = require('../../utils/EnvUtils').default,
    getValue = _require$default.getValue,
    Keys = _require$default.Keys;

var BaseHost = require('../BaseHost');
//系统运行时缺省环境参数
var DefaultParams = {
    //'a':'aaa',
    //'b':222,
    //'c':function(){
    //    return 'cccc'
    //}
    'baseUrl': BaseHost,
    'userId': function userId() {
        return getValue(Keys.USER_ID);
    },
    'userName': function userName() {
        return getValue(Keys.USER_NAME);
    },
    'orgId': function orgId() {
        return getValue(Keys.ORGA_ID);
    },
    'currDate': function currDate() {
        return getValue(Keys.CURRENT_DATE);
    },
    'currTime': function currTime() {
        return getValue(Keys.CURRENT_TIME);
    }
};

module.exports = DefaultParams;
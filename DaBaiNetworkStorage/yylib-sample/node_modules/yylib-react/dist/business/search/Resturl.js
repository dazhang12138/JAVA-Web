"use strict";

/**
 * Created by wuhao on 16/9/21.
 */
var ADDR = require('../BaseHost');
window.EnvConfig = window.EnvConfig || {};
var SUPPORT_BASEHOST = window.EnvConfig.SUPPORT_BASEHOST;
if (!SUPPORT_BASEHOST) {
    SUPPORT_BASEHOST = ADDR;
}
var urls = {
    REFER_SEVER_URL: ADDR,
    SEARCH_TEMPLATE: SUPPORT_BASEHOST + "/icop-support-web/searchTemplate", // 搜索模版管理
    SEARCH_ENTITITES: ADDR + "/icop-metadata-service/common/findEntities" // 搜索
};
module.exports = urls;
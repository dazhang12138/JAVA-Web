'use strict';

/**
 * Created by dongsuo on 2016/5/5.
 */
var React = require('react');

var _require = require('yylib-ui'),
    YYClass = _require.YYClass,
    YYIcon = _require.YYIcon;

var YYSearchSpan = YYClass.create({
    getDefaultProps: function getDefaultProps() {
        return {
            items: [//显示标签项
                //{
                //    key: string, //标识键
                //    title: string, //标题
                //    text: string  //文本
                //}
            ],
            onRemove: null //当项目被移除时的回调函数,传递参数：object-被移除的项目，array-剩余的项目
        };
    },
    handleOnClick: function handleOnClick(item) {
        this.props.onRemove(item);
    },
    render: function render() {
        var that = this;
        var items = this.props.items.map(function (item, index) {
            return React.createElement(
                'div',
                { className: 'yyui-filter-tag yyui-text-break', onClick: that.handleOnClick.bind(that, item) },
                React.createElement(
                    'span',
                    { className: 'yyui-filter-tag-title' },
                    item.title
                ),
                '\uFF1A',
                React.createElement(
                    'span',
                    { className: 'yyui-filter-tag-text' },
                    item.text
                ),
                React.createElement(YYIcon, { type: 'cross', style: { paddingTop: 3 } })
            );
        });
        return React.createElement(
            'div',
            { className: 'yyui-filter-tags' },
            items
        );
    }
});
module.exports = YYSearchSpan;
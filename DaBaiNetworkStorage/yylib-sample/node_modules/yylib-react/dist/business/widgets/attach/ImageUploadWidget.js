'use strict';

var React = require('react');
var _ = require('lodash');

var _require = require('yylib-ui'),
    YYClass = _require.YYClass,
    YYButton = _require.YYButton;

var EnvUtils = require('yylib-utils/EnvUtils').default;
var field = YYClass.field;
var PropTypes = YYClass.PropTypes;
var ImageUpload = require('../../attach/ImageUpload');
var ADDR = require('../../BaseHost');

var uiMeta = {
    name: "ImageUploadWidget",
    uiMeta: [field('serverUrl', ADDR, PropTypes.string, true, '上传地址'), field('value', '', PropTypes.string, true, ''), field('fileUrl', ADDR, PropTypes.string, false, ''), field('height', 100, PropTypes.number, true, ''), field('width', 100, PropTypes.number, true, ''), field('sourceId', '', PropTypes.string, false, ''), field('sourceType', '', PropTypes.string, false, ''), field('billType', null, PropTypes.string, true, '单据类型'), field('title', '点击上传', PropTypes.string, false, '')]
};
//已过期请使用新的YYImageWall组件
var ImageUploadWidget = YYClass.create({
    uiMeta: uiMeta,
    render: function render() {
        console.warn('本图片上传组件已过期，请删除并重新配置新的图片上传');
        return React.createElement(ImageUpload, this.props);
    }
});

module.exports = ImageUploadWidget;